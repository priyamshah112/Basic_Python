"""GUI FOR RECORDING FORM   FRONTEND   PAGE 1   LOGIN PAGE"""
from tkinter import *
def screen():
    
    
    window = Tk()
    
    frame=Frame(bg='green')
    lbl1=Label(window,text='\t\t ~~WELCOME TO K J SOMAIYA COLLEGE OF ENGINEERING~~ ')
    lbl1.pack()
    lbl1=Label(window,text='\t ~STUDENT RECORD FORM~' )
    lbl1.pack()

    lbl1=Label(window,text='1]PHYSICS' )
    lbl1.pack()
    lbl1=Label(window,text='ATTENDANCE' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    lbl1=Label(window,text='MARKS IN UT' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    lbl1=Label(window,text='MARKS IN SEM' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    
    lbl1=Label(window,text='2]CHEMISTRY' )
    lbl1.pack()
    lbl1=Label(window,text='ATTENDANCE' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    lbl1=Label(window,text='MARKS IN UT' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    lbl1=Label(window,text='MARKS IN SEM' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    
    lbl1=Label(window,text='3]MATHEMATICS' )
    lbl1.pack()
    lbl1=Label(window,text='ATTENDANCE' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    lbl1=Label(window,text='MARKS IN UT' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    lbl1=Label(window,text='MARKS IN SEM' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    
    lbl1=Label(window,text='4]ENGINEERING DRAWING' )
    lbl1.pack()
    lbl1=Label(window,text='ATTENDANCE' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    lbl1=Label(window,text='MARKS IN UT' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    lbl1=Label(window,text='MARKS IN SEM' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    
    lbl1=Label(window,text='5]CS' )
    lbl1.pack()
    lbl1=Label(window,text='ATTENDANCE' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    lbl1=Label(window,text='MARKS IN UT' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    lbl1=Label(window,text='MARKS IN SEM' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    
    lbl1=Label(window,text='6]BEEE' )
    lbl1.pack()
    lbl1=Label(window,text='ATTENDANCE' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    lbl1=Label(window,text='MARKS IN UT' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()

    lbl1=Label(window,text='MARKS IN SEM' )
    lbl1.pack()
    
    k=IntVar()
    e=Entry(window,textvariable=k)
    e.pack()
    window=mainloop()
screen()
